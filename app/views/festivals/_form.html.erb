


<%= form_for(@festival) do |f| %>
<% if @festival.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@festival.errors.count, "error") %> prohibited this festival from being saved:</h2>

  <ul>
    <% @festival.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>
  <!--
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label :status %><br>
    <%= f.check_box :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
    <% end %>
    //-->


    <div class="row">
      <form class="col s12">
        <div class="row">
          <div class="input-field col s12">
            <input id="name" type="text" class="active" name="name">
            <label for="name">Nom Du Festival</label>
          </div>
        </div>

        <div class="row">
          <div class="input-field col s12">
            <label for="date" class="active">Date Du Festival</label>
            <input name="date" type="date" class="datepicker">

          </div>
          <div class="input-field col s12">
            <label for="status" class="active">Status Des Candidatures</label>
            <select>

              <option value="ouvert">OUVERT</option>
              <option value="ferme">FERME</option>
            </select>
          </div>

          <div class="row">
            <div class="col s12">
              <form action="#">
               <div class="file-field input-field">
                 <div class="btn">
                   <span>Image</span>
                   <input type="file">
                 </div>
                 <div class="file-path-wrapper">
                   <input class="file-path validate" type="text">
                 </div>
               </div>
             </form>
           </div>
         </div>
       </form>
       <div class="row">
        <div class="col s12">
          <button class="btn waves-effect waves-light" type="submit" name="action">Créer
            <i class="material-icons right">send</i>
          </button>
        </div>
      </div>
    </div>



    <script>$(document).ready(function() {
      $('select').material_select();
    });
    $('.datepicker').pickadate({
    selectMonths: true, // Creates a dropdown to control month
    selectYears: 2 // Nombre d'années affichées-proposées sur le dropdown 15 par default (useless)
  });
    </script>